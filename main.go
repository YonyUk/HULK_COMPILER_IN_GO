package main

// "fmt"

// . "hulk.com/app/grammar"
// . "hulk.com/app/regex"

func main() {

	// StartSymbol := NewGrammarSymbol("S", NonTerminal)
	// s1 := NewGrammarSymbol("a", Terminal)
	// s2 := NewGrammarSymbol("b", Terminal)
	// G := NewGrammar(StartSymbol)
	// G.AddProduction(StartSymbol, []IGrammarSymbol{StartSymbol, s1})
	// G.AddProduction(StartSymbol, []IGrammarSymbol{StartSymbol, s2})

	// engine := NewRegexEngine()
	// aut, err := engine.Regex(G)
	// fmt.Println(err)

	// chain := "abababab"

	// for _, char := range chain {
	// 	fmt.Println(aut.Walk(char))
	// }
	// fmt.Println(aut.CurrentState())
}
